#include <bits/stdc++.h>
 
using namespace std;
 
int main(){
    string s;
    cin >> s;
    if(s.size() == 1 && s[0] == 'a'){
	cout << -1 << endl;
    }else{
	cout << 'a' << endl;
    }
    return 0;
}
